
-- PcimapResultWidget'e ek parametre olarak Mainboard ismini gonderebilirsin, boylece ayrica save mainboard butonunu 
da disable edebilirsi!!!

- In PcimapResultWidget;
	** Mark the devices as (no driver in any os, some OSes missing driver, all has driver), preferably using an additional column with colors to indicate it ;)
	* Filtering on Device Details; probably with OS or Kernel..

- if we want to delete an OS; the corresponding entries from `pcimap` and `modules` should be deleted first..

- DATABASE
	** Classes and Devices sections in database needs update procedure!!!!!

